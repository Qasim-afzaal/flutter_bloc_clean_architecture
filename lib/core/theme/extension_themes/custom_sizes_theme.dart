
import 'package:flutter_clean_architecture/core/theme/extensions/custom_sizes.dart';

class CustomSizesTheme {
  CustomSizesTheme._();

  static const CustomSizes lightTheme = CustomSizes(
    padding: 20,
    borderRadius: 15,
    formHeight: 30,
  );

  static const CustomSizes darkTheme = CustomSizes(
    padding: 20,
    borderRadius: 15,
    formHeight: 30,
  );
}
